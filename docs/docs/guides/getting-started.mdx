---
title: Getting Started
description: Install pgxcli and connect to your first database in under a minute.
keywords: [install pgxcli, postgres client, postgresql cli, go postgres, install psql alternative]
sidebar_position: 1
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## Installation

Pick your operating system or preferred installation method:

<Tabs>
  <TabItem value="linux" label="Linux">
    Download the package for your distro from the [releases page](https://github.com/balajz/pgxcli/releases).

    **Debian / Ubuntu**
    ```bash
    sudo dpkg -i pgxcli_*_linux_amd64.deb
    ```

    **Fedora / RHEL**
    ```bash
    sudo rpm -i pgxcli_*_linux_amd64.rpm
    ```

    **Alpine**
    ```bash
    sudo apk add --allow-untrusted pgxcli_*_linux_amd64.apk
    ```

    **Arch Linux**
    You can install from the [AUR](https://aur.archlinux.org/packages/pgxcli):
    ```bash
    yay -S pgxcli
    ```
    Or manually install the downloaded package:
    ```bash
    sudo pacman -U pgxcli_*_linux_amd64.pkg.tar.zst
    ```

    **Archive (tar.gz)**
    ```bash
    tar -xzf pgxcli_*_linux_amd64.tar.gz
    sudo mv pgxcli /usr/local/bin/
    ```
  </TabItem>
  <TabItem value="mac" label="Mac">
    Grab the latest `.tar.gz` for your architecture from the [releases page](https://github.com/balajz/pgxcli/releases).

    **Manual Installation**
    ```bash
    # For Apple Silicon (M1/M2/M3)
    tar -xzf pgxcli_0.3.0_darwin_arm64.tar.gz
    sudo mv pgxcli /usr/local/bin/

    # For Intel Mac
    tar -xzf pgxcli_0.3.0_darwin_amd64.tar.gz
    sudo mv pgxcli /usr/local/bin/
    ```
  </TabItem>
  <TabItem value="windows" label="Windows">
    **Installer (MSI)** — recommended

    Download the `.msi` from the [releases page](https://github.com/balajz/pgxcli/releases) and run it. pgxcli will be added to your `PATH` automatically.

    **Archive (zip)**

    Grab the `.zip` from the [releases page](https://github.com/balajz/pgxcli/releases), extract it, and place `pgxcli.exe` anywhere on your `PATH`.
  </TabItem>
  <TabItem value="go-install" label="Go Install">
    Make sure you have **Go 1.26.4+**.

    ```bash
    go install github.com/balajz/pgxcli@latest
    ```
  </TabItem>
  <TabItem value="docker" label="Docker">
    You can run pgxcli directly using Docker from GitHub Container Registry or Docker Hub:

    **GitHub Container Registry (GHCR)**
    ```bash
    docker run -it --rm ghcr.io/balajz/pgxcli:v0.3.0 postgres://user:pass@host:5432/db
    ```

    **Docker Hub**
    ```bash
    docker run -it --rm balajz/pgxcli:v0.3.0 postgres://user:pass@host:5432/db
    ```
  </TabItem>
  <TabItem value="source" label="Source Code">
    Make sure you have **Go 1.26.4+**.

    ```bash
    git clone https://github.com/balajz/pgxcli.git
    cd pgxcli
    make build
    ```

    The binary lands in `bin/app`. Move it wherever you like (e.g., `/usr/local/bin/pgxcli`).
  </TabItem>
</Tabs>

---

## Quick Start

Connect to a database:

```bash
pgxcli mydb myuser
```

That's it. You'll get a REPL with syntax highlighting, autocompletion, and history — ready to go.

:::tip
On first run, pgxcli creates a config file at `$XDG_CONFIG_HOME/pgxcli/config.toml` when `XDG_CONFIG_HOME` is set, otherwise in the OS-standard user config directory. You can customize your prompt, theme, pager, and more. See [Configuration](/docs/guides/configuration).
:::

### Try a Query

```sql
SELECT version();
```

Results render in a clean table with execution time. If the output is long, pgxcli pages it automatically.

### Exit

Type `\q` or press `Ctrl+C` twice.

---

## Next Steps

- [Connecting](/docs/guides/connecting) — all the ways to connect (flags, URI, interactive form)
- [Configuration](/docs/guides/configuration) — customize your prompt, theme, and behavior
- [Special Commands](/docs/guides/special-commands) — backslash commands reference
- [CLI Flags](/docs/reference/cli-reference) — every flag, documented
